export {};

// The previous declaration of JSX.IntrinsicElements was overriding the default React definitions,
// causing standard HTML elements to be missing from the JSX namespace.
// We are removing it to restore standard behavior since the custom element is unused.
